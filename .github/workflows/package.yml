name: Package nested templates

on:
  push:
    branches: [main]
    paths:
      - 'templates/**'
      - 'deployment/main.yaml'
      - '.github/workflows/package.yml'
  workflow_dispatch:

permissions:
  contents: write
  id-token: write

concurrency:
  group: package-${{ github.ref }}
  cancel-in-progress: false

jobs:
  package:
    name: Lint and package
    runs-on: ubuntu-latest
    steps:
      - name: Check out the repository
        uses: actions/checkout@v4

      - name: Lint every template
        run: |
          pip install --quiet cfn-lint
          cfn-lint templates/bootstrap.yaml templates/nested-stacks/*.yaml
          cfn-lint templates/main.yaml --ignore-checks W3002

      - name: Assume the packaging role
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_PACKAGE_ROLE_ARN }}
          aws-region: ${{ vars.AWS_REGION }}

      - name: Package the root template
        run: |
          aws cloudformation package \
            --template-file templates/main.yaml \
            --s3-bucket "${{ vars.TEMPLATE_BUCKET }}" \
            --s3-prefix nested-stacks \
            --output-template-file packaged/main.yaml

      - name: Validate the packaged deployment contract
        run: |
          cfn-lint packaged/main.yaml --ignore-checks W3002
          ruby <<'RUBY'
          require "yaml"

          deployment = YAML.load_file("deployment/main.yaml")
          packaged = YAML.load_file("packaged/main.yaml")
          configured = deployment.fetch("parameters", {}).keys
          available = packaged.fetch("Parameters", {}).keys
          missing = configured - available

          abort("deployment parameters missing from packaged template: #{missing.join(', ')}") unless missing.empty?
          puts "Deployment parameter contract OK"
          RUBY

      - name: Commit the packaged template
        run: |
          git add packaged/main.yaml

          if git diff --cached --quiet -- packaged/main.yaml; then
            echo "Packaged template unchanged - no nested stack templates were modified"
            exit 0
          fi

          git config user.name  'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git commit -m "chore: package nested templates for ${GITHUB_SHA::7}"
          git push
